#!/bin/bash
# Time-stamp: <2012-12-02 13:52:41 leo>

# Contains functions used to have a nice prompt.


function git_repo_name
# If the directory we are in is not part of a git branch, returns the
# empty string. Otherwise, returns the branch name.
{
    
}
